/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'localhost',
          'kb-landrup-dans-api.onrender.com'
            
        ]
    }
};

export default nextConfig;
