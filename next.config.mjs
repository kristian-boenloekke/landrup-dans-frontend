/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'kb-landrup-dans-api.onrender.com'
            
        ]
    }
};

export default nextConfig;
